# 2026-06-29 避雷器 UAVGen 复现工作日志

## 今日目标

围绕论文 `Visual Prototype Conditioned Focal Region Generation for UAV-Based Object Detection` 的思路，先做避雷器类别的数据增强准备工作。当前路线采用 object-preserved generation：真实避雷器主体保持，扩散模型只做边界、光照、阴影和背景过渡修复。

## 一阶段完成情况

### 数据与模型

- 数据配置：`/root/autodl-tmp/bileiqi/data.yaml`
- 避雷器 teacher detector：`/root/ultralytics/models/arrester.pt`
- YOLO 环境：`conda yolo26`
- 数据集规模：
  - train：85 张，142 个避雷器目标
  - val：21 张，35 个避雷器目标
  - 总计：106 张，177 个避雷器目标

### 标签与统计

已完成标签检查和统计：

- 标签错误数：0
- bbox/面积/长宽比/中心点分布已统计
- 目标位置热力图已生成
- focal crop 已生成
- 5x context crop 已生成
- layout pool 已生成

关键产物：

```text
/root/workspace/outputs/arrester/stage1/stage1_report.md
/root/workspace/outputs/arrester/stage1/dataset_stats.json
/root/workspace/outputs/arrester/stage1/label_check_report.json
/root/workspace/outputs/arrester/stage1/all_objects.jsonl
/root/workspace/outputs/arrester/stage1/focal_crops/
/root/workspace/outputs/arrester/stage1/context_crops_5x/
/root/workspace/outputs/arrester/stage1/layout_pool.json
/root/workspace/outputs/arrester/stage1/plots/
```

### Teacher detector 评估

已用 `/root/ultralytics/models/arrester.pt` 在 val 集上评估：

```text
Precision: 0.988
Recall: 0.743
mAP50: 0.791
mAP50-95: 0.435
```

结论：该模型可以作为二阶段的弱 teacher / 初筛器，用于判断生成图中避雷器是否存在、bbox 是否大致对齐、置信度是否过低。但它不适合作为最终强质量裁判，尤其不能判断避雷器与绝缘子混淆。

关键产物：

```text
/root/workspace/outputs/arrester/stage1/teacher_eval_report.json
/root/workspace/outputs/arrester/stage1/teacher_feedback.jsonl
/root/workspace/outputs/arrester/stage1/keep_samples.txt
/root/workspace/outputs/arrester/stage1/review_samples.txt
/root/workspace/outputs/arrester/stage1/drop_samples.txt
```

### 背景标签

已生成 focal crop 级自动背景标签：

```text
/root/workspace/outputs/arrester/stage1/background_tags_auto.jsonl
/root/workspace/outputs/arrester/stage1/background_tags_review.csv
```

当前决定：不再人工标注 `manual_tags`，第一版直接使用 `auto_tags` 作为弱背景提示。注意：`auto_tags` 只能用于粗匹配，不能作为硬语义标签。

自动标签示例包括：

```text
sky
vegetation
wire_or_tower
tower_or_equipment
equipment_dense
insulator_possible_review
```

其中 `insulator_possible_review` 只是提醒该 crop 可能含绝缘子/复杂设备结构，并不表示一定存在绝缘子。

## Prototype 筛选

### 初始候选

根据 teacher feedback 生成了严格候选：

```text
/root/workspace/outputs/arrester/stage1/prototype_candidates_strict.txt
/root/workspace/outputs/arrester/stage1/prototype_candidate_review_strict21.csv
```

严格条件：

```text
IoU >= 0.9
confidence >= 0.85
```

用户在 Windows 上标注后上传：

```text
/root/workspace/outputs/arrester/stage1/prototype_candidate_review_strict21.xlsx
```

标注结果：

```text
总行数: 29
A: 13
B: 13
C: 3
```

说明：用户额外补充了几个认为高质量的 A 级样本，已验证这些 `crop_id` 都能在 `focal_crops/` 和 `all_objects.jsonl` 中找到。

## 二阶段完成情况

### Prototype Bank

已根据用户标注的 xlsx 构建正式 prototype bank：

```text
/root/workspace/outputs/arrester/prototypes/bank/prototype_bank.json
/root/workspace/outputs/arrester/prototypes/bank/images/
/root/workspace/outputs/arrester/prototypes/bank/thumbnails/
/root/workspace/outputs/arrester/prototypes/bank/prototype_review_merged.csv
/root/workspace/outputs/arrester/prototypes/bank/README.md
```

结果：

```text
总可用 prototype: 26
A 级 core prototype: 13
B 级 backup/complex prototype: 13
C 级已排除: 3
```

策略：

- A 级：第一轮生成优先使用
- B 级：作为复杂/遮挡场景备选，低权重使用
- C 级：第一轮不使用

### Layout 与生成清单

已使用全部 177 个真实标注构建 layout pool：

```text
/root/workspace/outputs/arrester/stage1/layout_pool.json
```

已准备第一批 200 个生成任务清单：

```text
/root/workspace/outputs/arrester/stage2/generation_manifest_v1.json
/root/workspace/outputs/arrester/stage2/generation_manifest_v1_preview.csv
```

生成组合逻辑：

```text
A/B prototype
+
真实 layout_pool 中采样的 bbox/位置/尺度
+
auto_tags 做弱背景匹配
+
后续使用 SAM mask 和 SD2 inpainting 做 object-preserved harmonization
```

已写配置：

```text
/root/workspace/uavgen_arrester/configs/model_paths.yaml
/root/workspace/uavgen_arrester/configs/stage2_generate.yaml
```

已写脚本：

```text
/root/workspace/uavgen_arrester/scripts/build_prototype_bank.py
/root/workspace/uavgen_arrester/scripts/prepare_generation_manifest.py
/root/workspace/plan/build_layout_pool_and_context.py
/root/workspace/plan/run_arrester_stage1.py
/root/workspace/plan/recompute_arrester_teacher_feedback.py
```

二阶段状态说明：

```text
/root/workspace/outputs/arrester/stage2/STAGE2_STATUS.md
```

## 当前依赖状态

用户准备下载：

```text
/root/models/sam/sam_vit_h_4b8939.pth
/root/models/diffusion/stable-diffusion-2-inpainting/
```

当前 `conda yolo26` 已有：

```text
torch
cv2
PIL
numpy
ultralytics
```

当前 `conda yolo26` 缺少：

```text
diffusers
transformers
accelerate
segment_anything
```

明天需要确认模型是否下载完成，并安装/切换到可运行 diffusion + SAM 的环境。

## 明天待办

1. 检查模型路径是否存在：

```text
/root/models/sam/sam_vit_h_4b8939.pth
/root/models/diffusion/stable-diffusion-2-inpainting/
```

2. 检查/安装依赖：

```text
diffusers
transformers
accelerate
segment_anything
```

3. 生成 prototype mask：

```text
输入: /root/workspace/outputs/arrester/prototypes/bank/images/
输出: /root/workspace/outputs/arrester/prototypes/bank/masks/
```

4. 做第一版 object-preserved composite：

```text
prototype + mask + layout/context crop
```

5. 使用 SD2 inpainting 做 harmonization：

- 保护避雷器主体
- 只修边界、光照、阴影、背景过渡
- 初始 denoise strength 建议 0.25，范围 0.15-0.35

6. 用 `/root/ultralytics/models/arrester.pt` 回检生成结果：

建议过滤规则：

```text
保留: confidence >= 0.5 且 IoU >= 0.4
优先保留: confidence >= 0.7 且 IoU >= 0.5
人工复查: confidence 0.3-0.5 或 IoU 0.3-0.5
丢弃: 检测不到或 confidence < 0.3
```

7. 小批量生成建议：

第一轮先生成 50-100 张检查质量，不要直接跑满 200 张或更多。

## 重要结论

- 一阶段核心任务已完成。
- 二阶段的数据资产层已完成。
- 当前可以进入 SAM mask + SD2 inpainting 生成阶段。
- `/root/ultralytics/models/arrester.pt` 可以作为避雷器弱 teacher 使用，但不是最终强质量裁判。
- 现阶段不做绝缘子实验，因此不强制训练多类别 teacher；但生成结果仍建议人工抽检。
